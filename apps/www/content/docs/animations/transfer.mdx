---
title: Transfer
description: A transfer animation.
author:
  name: lucas
  url: https://github.com/lucasporrini
---

<ComponentPreview name="transfer-demo" />

## Installation

<ComponentInstallation name="transfer" />

## Usage

```tsx
<Transfer
  firstChild={
    <div className="z-10 flex items-center justify-center bg-white border border-gray-100 rounded-md size-10 p-1">
      <div className=" bg-white shadow-md w-full h-full flex items-center justify-center rounded-md">
        <Icons.gitHub className="size-6" />
      </div>
    </div>
  }
  secondChild={
    <div className="z-10 flex items-center justify-center bg-white border border-gray-100 rounded-md size-10 p-1">
      <div className=" bg-white shadow-md w-full h-full flex items-center justify-center rounded-md">
        <Icons.apple className="size-6" />
      </div>
    </div>
  }
  animation
/>
```

## Props

<TypeTable
  type={{
    speed: {
      description: "The particles speed.",
      type: "object",
      required: false,
      default: "{ min: 0, max: 1.2 }",
    },
    size: {
      description: "The size of the particles.",
      type: "object",
      required: false,
      default: "{ min: 6, max: 12 }",
    },
    secondChild: {
      description: "The second child to transfer.",
      type: "React.ReactNode",
      required: false,
    },
    opacity: {
      description: "The opacity of the particles.",
      type: "number",
      required: false,
      default: "1",
    },
    maxCurve: {
      description: "The maximum curve of the particles.",
      type: "number",
      required: false,
      default: "18",
    },
    firstChild: {
      description: "The first child to transfer.",
      type: "React.ReactNode",
      required: false,
    },
    duration: {
      description: "The duration of the particles.",
      type: "object",
      required: false,
      default: "{ min: 1.2, max: 1.8 }",
    },
    direction: {
      description: "The direction of the particles.",
      type: "string",
      required: false,
      default: "right",
    },
    delay: {
      description: "The delay between particles generation.",
      type: "number",
      required: false,
      default: "40",
    },
    containerWidth: {
      description:
        "The width of the container that will contain the particles.",
      type: "string",
      required: false,
      default: "100%",
    },
    containerHeight: {
      description:
        "The height of the container that will contain the particles.",
      type: "number",
      required: false,
      default: "40",
    },
    containerClassName: {
      description: "The class name of the container.",
      type: "string",
      required: false,
      default: "default",
    },
    color: {
      description: "The color of the particles.",
      type: "string[]",
      required: false,
      default: "['#000']",
    },
    animation: {
      description: "Whether to animate the transfer.",
      type: "boolean",
      required: false,
      default: "true",
    },
  }}
/>

## Credits

- Credits to [Lucas](https://github.com/lucasporrini) for the button style.
