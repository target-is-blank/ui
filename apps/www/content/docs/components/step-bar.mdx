---
title: Step Bar
description: A step bar indicator
author:
  name: lucas
  url: https://github.com/lucasporrini
---

<ComponentPreview name="step-bar-demo" />

## Installation

<ComponentInstallation name="step-bar" />

## Usage

```tsx
<StepBar steps={3} currentStep={1} />
```

## Props

<TypeTable
  type={{
    stepComponent: {
      description: "The component used to render each step.",
      type: "React.ElementType<T>",
      required: false,
      default: "div",
    },
    stepClassName: {
      description: "The class name to apply to the each step.",
      type: "string",
      required: false,
      default: "",
    },
    steps: {
      description: "The number of steps to display.",
      type: "number",
      required: true,
      default: 0,
    },
    size: {
      description: "The prebuilt size of the step bar.",
      type: "sm | md | lg",
      required: false,
      default: "md",
    },
    tooltipKeepVisible: {
      description:
        "Whether the tooltip should stay visible when the step is hovered.",
      type: "boolean",
      required: false,
      default: false,
    },
    tooltipContent: {
      description: "The content to display in the tooltip.",
      type: "(index: number) => string",
      required: false,
    },
    tooltipClassName: {
      description: "The class name to apply to the tooltip.",
      type: "string",
      required: false,
    },
    onStepLeave: {
      description: "The function to call when the step is left.",
      type: "() => void",
      required: false,
    },
    onStepHover: {
      description: "The function to call when the step is hovered.",
      type: "(index: number) => void",
      required: false,
    },
    onCurrentStepLeave: {
      description: "The function to call when the current step is left.",
      type: "() => void",
      required: false,
    },
    onCurrentStepHover: {
      description: "The function to call when the current step is hovered.",
      type: "(index: number) => void",
      required: false,
    },
    lastStepVisible: {
      description: "Whether the last step should be visible.",
      type: "boolean",
      required: false,
      default: true,
    },
    finished: {
      description: "The state used to indicate that the step bar is finished.",
      type: "boolean",
      required: false,
      default: false,
    },
    currentStep: {
      description: "The current step.",
      type: "number",
      required: true,
    },
    containerClassName: {
      description: "The class name to apply to the container.",
      type: "string",
      required: false,
    },
    color: {
      description: "The color of the step bar.",
      type: "string",
      required: false,
      default: "#000",
    },
  }}
/>
